= simple_form_for(@wallet) do |f|
  = f.error_notification

  .form-inputs
    = f.association :exchange
    = f.association :coin
    = f.input :balance
    = f.input :available
    = f.input :pending
    = f.input :buy_price

  .form-actions
    = f.button :submit
